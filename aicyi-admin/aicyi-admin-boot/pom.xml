<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.aicyi.admin</groupId>
        <artifactId>aicyi-admin</artifactId>
        <version>0.0.1-SNAPSHOT</version>
    </parent>

    <artifactId>aicyi-admin-boot</artifactId>
    <packaging>jar</packaging>
    <name>aicyi admin boot</name>
    <description>可执行启动模块</description>

    <dependencies>
        <dependency>
            <groupId>io.github.aicyi.admin</groupId>
            <artifactId>aicyi-admin-web</artifactId>
        </dependency>

        <!-- aicyi 中间件自动装配入口（Redis / 雪花ID / MyBatis-Plus 等按需启用） -->
        <dependency>
            <groupId>io.github.aicyi.framework</groupId>
            <artifactId>aicyi-framework-core-spring-boot-starter</artifactId>
        </dependency>

        <!-- Nacos 服务注册与发现 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <!-- Nacos 配置中心 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-config</artifactId>
        </dependency>

        <!-- 启用 bootstrap.yml 引导（namespace 需在 bootstrap 阶段读取） -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-bootstrap</artifactId>
        </dependency>

        <!-- MySQL 驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- maven-compiler-plugin（release 17 + lombok/mapstruct 注解处理器）由 aicyi-base-starter-parent 统一配置，
                 子模块禁止重复声明插件版本与 annotationProcessorPaths -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>io.github.aicyi.admin.boot.AdminApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
