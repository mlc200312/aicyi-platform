# aicyi 平台 生产上线验收报告

> **📌 2026-09-26 架构变更追记**：aicyi-admin 五模块已并入 aicyi-auth 单体（18090 承载认证+系统管理；18091 退役；网关 /api/system/** 与权限 RPC 改流 lb://aicyi-auth）。回归基线更新为**后端 42 用例**（S0 减 18091 探活）+ 前端 13 用例，合并后复跑全绿。

**验收日期**: 2026-09-25
**验收范围**: 前后端自动化回归（后端六服务 API + 前端核心旅程）
**结论**: ✅ **自动化功能回归全绿，达到上线验收标准**（上线前置条件见第五节）

---

## 一、测试套件（可重复执行）

| 套件 | 位置 | 命令 | 耗时 |
|---|---|---|---|
| 后端 API 回归 | `aicyi-platform/tests/api-regression.sh` | `./api-regression.sh`（完整）/ `--fast`（跳过吊销窗口等待） | ~2min / ~30s |
| 前端 E2E | `aicyi-platform-ui/tests/e2e/` | `npx playwright test`（webServer 自动托管 vite，channel=chrome 免浏览器下载） | ~19s |

前置条件：docker（mysql/redis/nacos/rabbitmq）+ 六服务（18000/18090-18094）启动；前端套件自动拉起 vite。

## 二、执行记录

| 轮次 | 后端 | 前端 |
|---|---|---|
| 首轮 | 30/37（脚本契约误判，逐项校准） | 10/13（选择器/断言校准） |
| **终验（背靠背复跑）** | **43 通过 / 0 失败 / 0 跳过** | **13 通过 / 0 失败** |

可重复性已验证：同环境连续两轮全绿。

## 三、覆盖矩阵

### 后端（6 段 43 用例）
| 分段 | 用例数 | 覆盖点 |
|---|---|---|
| S0 环境 | 6 | 六服务端口存活 |
| S1 认证令牌 | 9 | 登录/错误密码拒绝/忘记密码防枚举/刷新签发/新令牌认证有效/无令牌 401 |
| S2 权限收口 | 7 | 零权限用户 4 类拒绝 40300/内部端点网关拦截/超管放行/权限映射 RPC（41 条） |
| S3 系统管理 | 6 | 用户/角色/菜单查询 + 用户 CRUD 回路（创建→查询→删除） |
| S4 消息中心 | 5 | 模板查询 + 模板 CRUD 回路 + 无效发送拦截 |
| S5 工单中心 | 2 | 分页/详情 |
| S6 审计日志 | 5 | 分页/详情/清理保留期守卫（未来时间拒绝）/**@OperLog 操作留痕异步落库** |
| S7 跨服务吊销 | 3 | 登出→旧令牌 40102/**admin 禁用用户→旧令牌跨服务 40102**/重新启用 |

### 前端（13 用例，全程零未捕获 JS 异常断言）
- 认证守卫 4：未登录重定向（含 redirect 回跳）/ 错误密码拒绝（不跳转不发令牌）/ 登录成功签发令牌 / 登出清除令牌
- 动态菜单 2：RBAC 一级目录渲染（系统管理/工单中心/日志管理）/ 二级菜单展开（用户/角色/菜单管理）
- 页面加载 7：工作台/用户/角色/菜单/操作日志/工单/消息模板 —— 表格渲染 + 零 pageerror

## 四、测试过程中发现的问题

| # | 问题 | 定级 | 状态 |
|---|---|---|---|
| 1 | 登录失败前端**无任何错误提示**（`login/index.vue` handleLogin 无 catch，静默拒绝） | UX 体验缺陷（安全语义正确：拒绝进入） | ⚠️ 未阻断，建议上线后首批修复 |
| 2 | 后端套件首轮暴露脚本契约误判（page vs list 等） | 测试脚本问题 | ✅ 已校准 |

## 五、上线前置条件（Checklist）

**必须完成（阻塞项）**
- [ ] 生产 profile 核对：`application-prod.yml` 的数据库/Redis/MQ/Nacos（prod namespace）指向与凭据
- [ ] `key-prefix` 三端一致确认（auth/admin/gateway 统一 `aicyi-cloud-admin-token`）——本次已验证 dev
- [ ] 全量稳定运行 1-2 周后：`aicyi.token.legacy-token-policy: accept → reject`（存量旧令牌收口）
- [ ] super_admin 初始密码强制更换 + 测试账号（tst_noperm/Test001/batchdel_*）下线
- [ ] HTTPS / 域名 / 网关对外暴露面收敛确认
- [ ] 日志清理策略参数复核（`aicyi.log.clean.min-age-days` 生产建议 90）

**强烈建议（不阻塞）**
- [ ] 前端登录失败提示补齐（见第四节 #1）
- [ ] 前端生产构建验证：`npm run build` + 产物部署冒烟（本次验收基于 dev server）
- [ ] `aicyi-event`（4 文件）与前端深度安全评审（本轮范围外）
- [ ] 网关匹配器「{param} 与字面子路径重叠 → 并集任一放行」隐患评估（工单等既有路由同权限码暂无风险）
- [ ] 性能压测、日志/监控告警接入（xxl-job 已就位）、备份恢复演练

## 六、遗留技术债（非阻塞，已在既往审计记录）

- log 列表接口返回 requestParam/responseResult 大字段（建议 list 剔除）
- 未配权限接口默认放行的存量策略（网关为唯一收口的设计决策，靠菜单注册覆盖度保障）
- forgot-password 邮件真实发送链路（消息服务消费 ForgotPasswordEvent）需生产 SMTP 验证

---

**验收签署**: 后端 43/43 ✅ · 前端 13/13 ✅ · 复跑可重复 ✅
